spring:
  cloud:
    config:
      #   配置服务器的默认地址为 localhost:8888，如果修改了默认地址，
      #则客户端项目就不能在 bootstrap.yml 或 application.properties 中配置 spring.cloud.config.uri，
      #必须在 bootstrap.yml 或是 bootstrap.properties 中配置，
      #原因是 bootstrap 开头的配置文件会被优先加载和配置，切记。
      uri: http://192.168.159.138:8888
      name: itoken-eureka
      label: main
      profile: prod